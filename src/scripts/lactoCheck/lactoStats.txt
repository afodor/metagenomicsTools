rm(list=ls())

setwd("C:\\lactoCheck")

myT <- read.table("Lacto_CrispatusPlusRunID.tsv", sep="\t", header=TRUE, row.names=1)

myT <- myT[myT$run == "Run1",]

plot( myT$L_crispatus ~ myT$group_number)

summary( lm(myT$L_crispatus ~ myT$group_number))