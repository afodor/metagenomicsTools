
rm(list=ls())

setwd("C:\\Jobin_April_2015")

myT <- read.table("hpcR_taxaAsColumns_mergedF_R_phylaLogNormalWithMetadata.txt", header=TRUE, sep="\t")

for( i in 7:col(myT))
{
	if( sum(myT[,i] >0 ) > nrow(myT)/4)
	{
		boxplot( myT[,i]  ~ myT$diseaseGroup)
	}
}
